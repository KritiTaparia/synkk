{
  "name": "synkk-server",
  "version": "1.0.0",
  "description": "Backend Server for all Synkk chrome extension calls",
  "main": "app.js",
  "scripts": {
    "start": "node --env-file=.env app.js",
    "watch": "nodemon --env-file=.env app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
